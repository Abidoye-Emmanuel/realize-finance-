import 'package:flutter/material.dart';

class PinCreation extends StatelessWidget {
  const PinCreation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}